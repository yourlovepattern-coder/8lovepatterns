{
  "name": "eightlovepatterns-site",
  "private": true,
  "type": "module",
  "description": "8LovePatterns site + serverless functions (Stripe webhook -> emailed report).",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
